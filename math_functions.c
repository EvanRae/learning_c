#include <stdio.h>
#include <math.h>  //for maths shiz

int main(){

    double A = sqrt(9); //square root function
    double B = pow(2, 4); //raises the first number to the power of the second number
    int C = round(3.14); //rounding numbers
    int D = ceil(3.14); //round number UP
    int E = floor(3.14); //round number DOWN
    double F = fabs(-100); //how far a number is away from 0 (Absolute number) (turns - to +)
    double G = log(3); //logarithms of numbers
    double H = sin(45); //triganomatry sin cos tan
    double I = cos(90);
    double J = tan(30);


    printf("\n%lf", H);

    return 0; 
}